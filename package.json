{
  "type": "module",
  "scripts": {
    "dev": "vitepress dev --host --port 3000",
    "build": "vitepress build",
    "preview": "vitepress preview --host --port 3000"
  },
  "devDependencies": {
    "vitepress": "^1.0.0-rc.31",
    "@vite-pwa/vitepress": "^0.3.1"
  }
}
